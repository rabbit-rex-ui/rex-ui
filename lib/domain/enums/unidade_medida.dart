/// Unidade de medida de um produto.
///
/// `un` — produto unitário (uma lata, um pacote). Quantidade sempre inteira.
/// `kg` — produto pesável (hortifruti, padaria). Quantidade com 3 casas
///        decimais (ex: 0,842).
enum UnidadeMedida { un, kg }
